apiVersion: v1
kind: Service
metadata:
  name: health-service
  labels:
    app: health-service
spec:
  type: ClusterIP
  selector:
    app: health-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
